<!-- Brightness modal (Android-like slider) -->
<div id="brightnessModal" class="brightness-modal" style="display:none; position:fixed; z-index:9999;">
  <div class="brightness-panel">
    <div class="brightness-title">Set animation brightness</div>
    <div class="brightness-reco">Recommended: <strong>80%</strong></div>
    <input id="brightnessRange" type="range" min="0" max="100" step="1" value="80">
    <div class="brightness-value"><span id="brightnessValue">80</span>%</div>
    <div class="brightness-actions">
      <button id="brightnessCancel">Cancel</button>
      <button id="brightnessApply">Apply</button>
    </div>
  </div>
</div>

<style>
/* small styles for the slider panel */
.brightness-modal .brightness-panel {
  background: #111;
  color: #fff;
  padding: 10px;
  border-radius: 8px;
  box-shadow: 0 6px 20px rgba(0,0,0,0.6);
  min-width: 220px;
  text-align: center;
  font-family: sans-serif;
}
.brightness-title { font-weight:600; margin-bottom:6px; }
.brightness-reco { font-size:12px; color:#ccc; margin-bottom:6px; }
#brightnessRange { width: 100%; margin: 8px 0; }
.brightness-value { font-weight:600; margin-bottom:8px; }
.brightness-actions button {
  margin: 0 6px; padding:6px 10px; border-radius:6px; border: none;
  background: #2b2b2b; color:white; cursor:pointer;
}
.brightness-actions button:hover { opacity:0.9; }
</style>
