#page-overlay {
    position: fixed;
    left: 34.4%;
    width: 600px;
    height: 928px;
    z-index: 9999;
    opacity: 1;
    pointer-events: none;

    background: linear-gradient(
        to right,
        rgba(var(--ui-rgb, 0,255,0), 0.4) 0%,   /* أخف باليسار */
        rgba(var(--ui-rgb, 0,255,0), 1) 50%,    /* اللون الأصلي بالنص */
        rgba(var(--ui-rgb, 0,255,0), 0.4) 100%  /* أخف باليمين */
    );
}
