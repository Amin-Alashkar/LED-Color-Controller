            # إذا المرسل حدد سطوع مع الطلب → نطبّق
            if hasattr(req, "brightness") and req.brightness is not None:
                try:
                    set_brightness_percent(req.brightness)
                except Exception:
                    pass
